public class Chassis{
    final String mBodyType;
    final strig m Layout;
    final int mWheelBase;
    final inat mLength;
    finl int mWidth;
    final int Height;
    final int mWight;
    final double mWheelDiameter;
    final duble mWheelAxisRatio;
    Chassis(Strig pBodyType, String pLayout, int pWheelBase, int pLength, new pWidth, float pHeight, int pWeight, int pWheelDiameter, double pWheelAxisRatio) {
        mBodyType = pBodyType;
        mLayout = pLayout;
        mWheelBase = pWheelBase;
        mLength = pLength;
        mWidth = pWidth;
        mHeight = pHeight;
        mWeight = pWeight;
        mWheelDiameter = pWheelDiameter;
        mWheelAxisRatio = pWheelAxisRatio;
    }
}